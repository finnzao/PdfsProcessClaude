# Extrator de PDFs jurídicos (PJe/TJBA) → Markdown otimizado para LLMs
#
# Instale com: pip install -r requirements.txt

# Núcleo: extração de texto de PDFs (rápido, com layout)
pymupdf4llm>=0.0.17
pymupdf>=1.24.0

# OCR fallback para páginas escaneadas (opcional, mas recomendado)
# Requer Tesseract instalado no sistema:
#   Linux:   sudo apt install tesseract-ocr tesseract-ocr-por
#   macOS:   brew install tesseract tesseract-lang
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
pytesseract>=0.3.10
Pillow>=10.0.0

# Pré-processamento de imagem para OCR (binarização Otsu)
numpy>=1.24.0
