# Extrator de PDFs juridicos (PJe/TJBA) -> Markdown otimizado para LLMs
#
# Instale com: pip install -r requirements.txt

# Nucleo: extracao de texto de PDFs (rapido, com layout)
pymupdf4llm>=0.0.17
pymupdf>=1.24.0

# OCR fallback para paginas escaneadas
# Requer Tesseract instalado no sistema:
#   Linux:   sudo apt install tesseract-ocr tesseract-ocr-por
#   macOS:   brew install tesseract tesseract-lang
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
pytesseract>=0.3.10
Pillow>=10.0.0

# Pre-processamento de imagem para OCR (Otsu, deskew)
numpy>=1.24.0
opencv-python-headless>=4.8.0

# Barra de progresso e UI no terminal
rich>=13.0.0

# Reconciliador de nomes (fuzzy matching)
rapidfuzz>=3.0.0
unidecode>=1.3.0

# Planilhas
openpyxl>=3.1.0
